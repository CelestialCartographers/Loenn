local cameraOffset = {}

cameraOffset.name = "cameraOffsetTrigger"
cameraOffset.category = "camera"
cameraOffset.placements = {
    name = "camera_offset",
    data = {
        cameraX = 0.0,
        cameraY = 0.0
    }
}

return cameraOffset